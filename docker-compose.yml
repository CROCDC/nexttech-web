version: '3.8'

services:
  web:
    build: .
    ports:
      - "160:160"
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
      - EMAIL_HOST=mailu.pantech.solutions
      - EMAIL_PORT=587
      - EMAIL_USER=info@pantech.solutions
      - EMAIL_PASSWORD=i3071948
      - EMAIL_USE_TLS=True
    volumes:
      - ./app:/app
    restart: unless-stopped
    networks:
      - pantech-network

networks:
  pantech-network:
    driver: bridge